# Reading Progress Tracker

Check the Python files to know required libraries.
Launch app.py (main.py required but not supposed to be launched).
So far you can add books (Title, Author, Category, Progress)
